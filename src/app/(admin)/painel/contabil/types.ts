export interface AccountingSummary {
  from: string;
  to: string;
  revenue: string;
  cogs: string;
  margin: string;
  payablesCreated: string;
  payablesPaid: string;
  payablesPending: string;
  receivablesOpen: string;
  receivablesReceived: string;
}
